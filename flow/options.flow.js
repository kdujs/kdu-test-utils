declare type Options = { // eslint-disable-line no-undef
    attachToDocument?: boolean,
    mocks?: Object,
    slots?: Object,
    localKdu?: Component,
    provide?: Object,
    stubs?: Object,
    context?: Object,
    attrs?: Object,
    listeners?: Object
}
